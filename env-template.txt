# Copy this file to `.env` and fill in your own values.
# NOTE: this template previously contained real credentials. If you cloned this
# repository before that was fixed, rotate the Cloudinary key/secret and the
# database password immediately.

# --- Database -----------------------------------------------------------------
DATABASE_URL="postgresql://USER:PASSWORD@localhost:5432/jobportal"

# --- Auth ---------------------------------------------------------------------
# Generate with: openssl rand -base64 48
JWT_SECRET="replace-me-with-a-long-random-string"

# --- Cloudinary (resume uploads) ----------------------------------------------
CLOUDINARY_CLOUD_NAME="your-cloud-name"
CLOUDINARY_API_KEY="your-api-key"
CLOUDINARY_API_SECRET="your-api-secret"

# --- Application --------------------------------------------------------------
NODE_ENV="development"
NEXT_PUBLIC_APP_URL="http://localhost:3000"

# --- Google sign-in (optional) ------------------------------------------------
# Leave blank to hide/disable the "Continue with Google" buttons.
GOOGLE_CLIENT_ID=""
GOOGLE_CLIENT_SECRET=""
NEXTAUTH_URL="http://localhost:3000"
# Generate with: openssl rand -base64 48
NEXTAUTH_SECRET="replace-me-with-a-long-random-string"

# --- Real-time chat (Socket.IO) -----------------------------------------------
# Port the standalone socket server listens on.
SOCKET_SERVER_PORT="3002"
# Where the Next.js API posts message events (server -> socket server).
SOCKET_SERVER_URL="http://localhost:3002"
# Where the browser connects. Real-time is disabled in production if unset.
NEXT_PUBLIC_SOCKET_URL="http://localhost:3002"
# Shared secret required by the socket server's /emit-message endpoint, so that
# only this app can inject messages into a conversation room.
SOCKET_EMIT_SECRET="replace-me-with-a-long-random-string"
# Comma-separated browser origins allowed to connect. Defaults to "*" in
# development; always set this in production.
SOCKET_ALLOWED_ORIGINS="http://localhost:3000"

# --- AI: Gemini (optional) ----------------------------------------------------
# Powers semantic job matching, semantic search, resume parsing and drafting
# help. Leave GEMINI_API_KEY blank to switch every AI feature off cleanly: the
# portal still works, match scores simply do not appear and search falls back to
# keyword filtering. Get a key at https://aistudio.google.com/apikey
GEMINI_API_KEY=""

# Optional overrides. gemini-embedding-001 supports asymmetric task types, which
# measurably improves query-vs-document retrieval.
GEMINI_EMBEDDING_MODEL="gemini-embedding-001"
GEMINI_TEXT_MODEL="gemini-2.5-flash"
# Stored vector width (128-3072). 768 keeps rows and the similarity loop about
# four times cheaper than the 3072 default for very little loss in quality.
GEMINI_EMBEDDING_DIMENSIONS="768"
GEMINI_TIMEOUT_MS="20000"
